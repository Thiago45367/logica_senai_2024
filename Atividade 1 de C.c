#include <stdio.h> //Texto obrigatório em qualquer programação.

// std -> standard=padrão
// i -> input=entrada
// o -> output=saída

// Função principal.
int main(){   // Main=Inicio(PORTUGOL)

	// Comando para mostrar no terminal.
	printf("hello, world!");   // Printf=Escreva(PORTUGOL)
	
	// Comando para pressionar uma tecla
	// antes de finalizar o programa.
	getchar();  
	
	// Comando para retornar o código 0.
	// Código usado para informar que o
	// programa finalizou corretamente.
	return 0; 
}
